[project]
name = "drawbot"
version = "0.1.0"
description = "Add your description here"
readme = "Readme.md"
requires-python = ">=3.10"
dependencies = [
    "baffi",
    "opencv-python>=4.12.0.88",
    "pypotrace>=0.3",
    "super-image>=0.2.0",
    "svg-to-gcode>=1.5.4",
    "svgpathtools>=1.7.1",
]

[dependency-groups]
dev = [
    "ruff>=0.14.6",
]
test = [
    "pytest>=9.0.1",
]

[tool.uv.sources]
baffi = { git = "https://github.com/MarcDuQuesne/baffi.git" }

[tool.pytest]
log_cli = true
log_cli_date_format = "%Y-%m-%d %H:%M:%S"
log_cli_format = "%(asctime)s %(levelname)s %(message)s"
log_cli_level = "DEBUG"